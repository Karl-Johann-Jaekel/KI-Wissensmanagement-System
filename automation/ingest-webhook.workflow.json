{
  "name": "KWMS — PDF Webhook → /ingest",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "ingest-pdf",
        "responseMode": "lastNode",
        "options": { "binaryData": true, "rawBody": true }
      },
      "id": "webhook-in",
      "name": "PDF Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 1,
      "position": [420, 300],
      "webhookId": "kwms-ingest-pdf"
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $env.KWMS_API_BASE }}/ingest",
        "sendQuery": true,
        "queryParameters": {
          "parameters": [
            { "name": "filename", "value": "={{ $json.query.filename || 'upload.pdf' }}" },
            { "name": "sensitivity", "value": "={{ $json.query.sensitivity || 'public' }}" }
          ]
        },
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            { "name": "X-API-Key", "value": "={{ $env.KWMS_ADMIN_KEY }}" },
            { "name": "Content-Type", "value": "application/pdf" }
          ]
        },
        "contentType": "binaryData",
        "inputDataFieldName": "data",
        "options": {}
      },
      "id": "http-ingest",
      "name": "POST /ingest",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4,
      "position": [680, 300]
    }
  ],
  "connections": {
    "PDF Webhook": {
      "main": [[{ "node": "POST /ingest", "type": "main", "index": 0 }]]
    }
  },
  "active": false,
  "settings": {},
  "tags": ["kwms"]
}
